models:
  - name: customer_orders
    columns:
      - name: customer_key
        tests:
          - unique
          - not_null:
              config:
                severity: error
                error_if: ">1000"
                warn_if: ">10"

